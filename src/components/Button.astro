---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  fullWidth?: boolean;
  class?: string;
  target?: string;
  rel?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  class: customClass = '',
  target,
  rel
} = Astro.props;

const baseStyles =
  'inline-flex items-center justify-center gap-2 rounded-full border text-sm font-semibold tracking-[0.01em] transition duration-200 ease-out focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--color-accent)] focus-visible:ring-offset-2 focus-visible:ring-offset-[color:var(--color-bg)]';

const variantStyles = {
  primary:
    'border-[color:var(--color-accent)] bg-[color:var(--color-accent)] text-[color:var(--color-ink-strong)] shadow-[0_10px_24px_rgba(183,155,104,0.24)] hover:-translate-y-0.5 hover:bg-[color:var(--color-accent-strong)] hover:border-[color:var(--color-accent-strong)]',
  secondary:
    'border-[color:var(--color-ink-strong)] bg-[color:var(--color-ink-strong)] text-white shadow-[0_12px_28px_rgba(24,37,45,0.18)] hover:-translate-y-0.5 hover:bg-[#22343d]',
  outline:
    'border-[color:var(--color-border-strong)] bg-white/75 text-[color:var(--color-ink-strong)] hover:-translate-y-0.5 hover:border-[color:var(--color-ink-strong)] hover:bg-white',
  ghost: 'border-transparent bg-transparent text-[color:var(--color-ink-strong)] hover:bg-white/70'
};

const sizeStyles = {
  sm: 'px-4 py-2.5',
  md: 'px-5 py-3',
  lg: 'px-6 py-3.5 text-[0.95rem]'
};

const widthClass = fullWidth ? 'w-full' : '';
const styleClass = `${baseStyles} ${variantStyles[variant]} ${sizeStyles[size]} ${widthClass} ${customClass}`;

const Tag = href ? 'a' : 'button';
---

<Tag
  href={href}
  class={styleClass}
  target={target}
  rel={rel}
  {...(href === undefined && { type: 'button' })}
>
  <slot />
</Tag>
