---
import { normalizeRichText } from '../lib/richText';

interface Props {
  html?: string;
  className?: string;
}

const { html = '', className = '' } = Astro.props;
---

<div class={`cms-richtext ${className}`.trim()} set:html={normalizeRichText(html)} />